1、数据增强：尝试一下翻转，股市预测的可逆性？
2、参考tokenizer的思路，训一个通用的latent space网络，后面接不同的预测头，强迫隐空间特征表示学到正确有价值信息，此隐空间能够刻画特征向量
3、怎么验证这个隐空间网络的有效性呢？通过降维可视化，相似的股票的欧式距离应该更近，比如同样是两个走势相似的股票数据在隐空间中的特征向量相近，反之同理。
4、通过LLM对相关新闻进行情绪评估，加入情绪向量，但是情绪向量怎么构建呢，有几个思路
4.1：s*1维度，s长度与序列长度一致，每天给一个情绪值0~5，3为中立，-1为缺失填充
4.2：1维度，仅提供最近一天的情绪值
4.3：